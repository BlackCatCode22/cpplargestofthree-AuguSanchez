#include <iostream>

int main() {
    int num1, num2, num3;
    
    std::cout << "Enter the first integer: ";
    std::cin >> num1;

    std::cout << "Enter the second integer: ";
    std::cin >> num2;

    std::cout << "Enter the third integer: ";
    std::cin >> num3;
    
    int sum = num1 + num2 + num3;
    
    int largest = num1; // Assume the first number is the largest
    
    if (num2 > largest) {
        largest = num2;
    }
    
    if (num3 > largest) {
        largest = num3;
    }
    
    std::cout << "Sum: " << sum << std::endl;
    std::cout << "Largest number: " << largest << std::endl;
    
    return 0;
}

//REFERNECE
//https://www.youtube.com/watch?v=pg4GDvoTeTQ
